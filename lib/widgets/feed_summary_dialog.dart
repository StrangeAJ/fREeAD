// Moved to `lib/screens/reader/feed_digest_sheet.dart` in v3. Kept so existing
// imports keep resolving `FeedSummaryDialog`; new code should call
// `showFeedDigestSheet(context)`.
export '../screens/reader/feed_digest_sheet.dart';
